# Something page directory

## Rules:

- Page file must named `Page.tsx`
- Page component must named `[PageName]Page`
- All components which are not used in other pages must be placed in `widgets` directory
